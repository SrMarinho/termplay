// games/velha/index.js — Jogo da Velha web view (placeholder until a renderer exists).
import { registerPlaceholder } from "../../core/placeholder.js";

registerPlaceholder("velha.state", "Jogo da Velha");
